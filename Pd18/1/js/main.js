const studentas ={
    vardas: "Denisas",
    pavarde:"Masinas",
    amzius: "18",
};

function sudeti(objektas){
    return (objektas.vardas+" "+objektas.pavarde)
}

console.log(sudeti(studentas))
